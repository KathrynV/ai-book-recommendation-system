{
  "name": "ai-book-recommendation-system",
  "version": "1.0.0",
  "description": "AI-powered book recommendation system using customer history, preferences, and inventory availability.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --use-system-ca --env-file-if-exists=.env server.js",
    "mcp-server": "node mcp/server.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.9.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
